# Reference: Celery worker is defined in the repo-root render.yaml (chess-insight-celery).
# This file is kept for documentation only — do not merge manually.
#
# Requires Render Starter+ (background workers are not available on the free tier).
# After upgrading, sync the Chessrun Blueprint from main.
